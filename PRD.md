# 取名 App · 产品需求文档（PRD）

> 版本：v1.0（第一版）
> 状态：已与用户确认，作为后续开发依据
> 运行形态：仅在本机浏览器本地运行的单页应用，无需联网、登录、部署、云数据库与手机端

---

## 1. 产品目标和使用场景

### 产品目标
做一个**只供本人使用**的取名专属工具，把"新生儿取名、生辰八字取名、诗词取名、宠物取名、名字解析及评分"五件事整合到一个本地 App 里。它是一台"取名工作台"：既能快速出名字，也能按八字、诗词、宠物等不同场景做针对性的取名，还能对任意名字进行解析打分，帮助判断一个名字好不好。

### 使用场景
- 家里或朋友家添了宝宝，想结合宝宝的**生辰八字、五行补益**起一个合适的名字。
- 想从**诗词名句**中找灵感，起一个文雅、有出处的名字。
- 给自家**宠物**起名，希望名字贴合宠物的类型、性格和气质。
- 起好或听到一个名字后，想**解析和评分**，看看寓意、音律、字形、五行、谐音是否靠谱。
- 手头有自己收集的诗文/文献素材，想**上传后提取好字好词**来取名。
- 以上所有操作都在自己电脑浏览器里完成，数据留在本机，随时可备份。

### 约束边界
- 仅在本机浏览器中本地运行，双击/本地打开即可使用。
- 数据保存在当前电脑，刷新、关闭、重新启动后不丢失。
- 不需要登录、联网、云数据库、部署和手机端。
- "AI 取名"= 离线规则 + 内置名字库智能组合，不联网、不接大模型接口。

---

## 2. 整体页面框架和导航结构

采用**单页应用 + 左侧常驻导航**框架。

### App 外壳（所有页面的公共框架）
| 区域 | 内容 |
|---|---|
| **顶栏** | 应用名 · 本地数据状态标识 · 备份导入导出 · 设置入口 |
| **左侧导航** | 常驻，点击切换 6 个主入口 |
| **主内容区** | 渲染当前页面，页面内部自行滚动 |
| **全局抽屉** | 收藏夹、历史记录（可从任意页面唤出） |

### 导航结构
```
取名 App
├── 首页
├── 新生儿取名
├── 生辰八字取名
├── 诗词取名
├── 宠物取名
└── 名字解析及评分
```

### 全局功能（所有页面可用）
- **收藏夹**：跨模块统一查看收藏的名字
- **数据备份**：一键导出 JSON / 导入恢复
- **历史记录**：所有操作自动留痕
- **偏好设置**：默认姓氏、常用风格等

---

## 3. 首页的内容结构

首页是"快速取名 + 各模块摘要"的工作台，**不是单纯菜单**。从上到下依次为：

1. **快速 AI 取名卡片**（最醒目）：选姓氏 → 选场景（新生儿 / 八字 / 宠物）→ 一键生成；生成结果在卡片内即时预览，无需跳页。
2. **上传文献取名卡片**：选择本地 txt → 提取好字好词 → 生成候选名（自备资料）。
3. **模块摘要卡片 ×5**：新生儿、八字、诗词、宠物、解析各自的重要摘要——最近记录、收藏数、常用偏好等。
4. **最近记录列表**：各模块最近一次取名，点击直达对应详情。

---

## 4. 每个模块的用途

| 模块 | 用途 |
|---|---|
| **新生儿取名** | 围绕「条件筛选 → 批量生成 → 收藏」给宝宝起名，支持避讳字过滤、家族字辈固定中间字 |
| **生辰八字取名** | 输入出生信息 → 本地排盘 → 按五行补益推荐名字，每个字标注五行属性 |
| **诗词取名** | 从诗词名句中提炼好字好词，每个名字带出处原文与释义 |
| **宠物取名** | 按宠物类型、性别、毛色、性格生成萌系 / 霸气 / 文艺等风格名字，支持中英文名 |
| **名字解析及评分** | 任意名字做多维度解析并打分，作为取名质量校验工具 |

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 新生儿取名
- **数据**：姓氏、性别、期望风格（文雅/大气/可爱/现代等）、避讳字、家族字辈（均为用户可填/可选输入）
- **操作**：
  1. 设置条件（姓氏、性别、风格、避讳字、字辈）
  2. 点「生成名字」→ 出结果
  3. 名字卡片展示拼音、笔画、五行、寓意
  4. 点「换一批」重新生成；支持按风格筛选 / 只看收藏
  5. 卡片星标即时收藏
  6. 历史按时间保存，可回看

### 5.2 生辰八字取名
- **数据**：出生日期（公历/农历）、出生时辰（可选）、性别、姓氏
- **操作**：
  1. 分步向导 Step1：输入出生信息
  2. Step2：本地排盘——四柱干支 + 五行统计 + 喜用神/缺失提示
  3. Step3：按五行补益推荐名字，标注每个字的五行属性
  4. 展示名字 + 五行匹配度 + 寓意，可收藏
- 说明：排盘全部本地完成，不联网；V1 用简化五行喜忌规则。

### 5.3 诗词取名
- **数据**：内置诗词库（按出处分类：诗经、楚辞、唐诗、宋词…）、诗句检索关键词、选中的名句
- **操作**：
  1. 浏览诗词库（按出处分类）
  2. 输入字/词实时检索过滤
  3. 点选名句 → 提炼关键字 → 组合候选名（所见即所得）
  4. 每个名字附原句出处与释义，可收藏

### 5.4 宠物取名
- **数据**：宠物类型（猫/狗/鸟/其他）、性别、毛色、性格、想要风格
- **操作**：
  1. 设置宠物信息
  2. 选择风格 → 切换对应风格词库（萌系叠词、食物系、拟声系、洋气音译等）
  3. 生成名字卡片：名字 + 含义 + 适合说明
  4. 支持中英文名切换展示
  5. 按宠物保存命名记录，可收藏

### 5.5 名字解析及评分
- **数据**：输入的名字（姓 + 名，单字/多字均可）
- **操作**：
  1. 输入名字即开始解析（实时反馈）
  2. 分项解析：字义寓意、五行、音律、字形、笔画、谐音检查
  3. 评分结果：分项评分 + 总分 + 一句话评语
  4. 自动提示不良谐音风险
  5. 历史保留解析过的名字与分数

---

## 6. 首页和各模块之间的关系

- 首页是**总入口和工作台**，各模块是具体执行场景。
- **快速 AI 取名卡片**：覆盖新生儿 / 八字 / 宠物三类场景的快捷入口，生成结果可直接预览，并可进入对应模块做深化（如继续筛选、查看详情）。
- **上传文献取名卡片**：产出"自备文献提炼出的好字好词"，可作为全局取名素材库，供各取名模块复用。
- **模块摘要卡片**：汇总每个模块的最近记录、收藏数、常用偏好，点击进入对应模块。
- **最近记录列表**：跨模块统一历史入口，点击直达对应模块详情。
- 各模块产生/收藏的记录统一进入全局收藏夹与历史记录。

---

## 7. 本地数据文件、备份和恢复要求

- **存储位置**：数据保存在本机浏览器本地存储（IndexedDB 优先，兼顾容量与持久性），刷新、关闭、重启均不丢失。
- **存储内容**：
  - 取名记录（各模块历史）
  - 收藏夹
  - 用户自定义素材（上传文献提取的字词）
  - 偏好设置
  - 内置基础数据库：常用汉字库（含笔画、五行、寓意）、常用名字库、诗词名句库、宠物词库
- **备份**：提供"一键导出"，将全部用户数据导出为 JSON 文件保存到本地。
- **恢复**：提供"导入"，选择之前导出的 JSON 文件可恢复数据。
- 内置数据库为随应用打包的只读基础数据；用户数据（记录/收藏/素材/偏好）与内置数据分离存储，备份只导出用户数据。

---

## 8. 第一版必须完成的功能

### 8.1 整体与数据层
- 单页应用，浏览器本地打开即可用，无需联网
- 本地持久化存储（IndexedDB），数据不丢失
- 内置基础数据库（汉字库、名字库、诗词名句库、宠物词库）
- 收藏夹、历史记录、偏好设置
- 一键导出 / 导入备份

### 8.2 首页
- 快速 AI 取名入口（内置取名数据库，输入姓氏/类型/偏好即可出名字）
- 上传文献取名入口（选择本地 txt → 提取好字好词 → 取名）
- 5 个模块的重要摘要卡片
- 最近记录列表

### 8.3 新生儿取名
- 条件设置（姓氏、性别、风格、避讳字、字辈）
- 基于内置库 + 规则智能生成名字，展示拼音/笔画/五行/寓意
- 换一批、风格筛选、只看收藏
- 收藏、历史保存

### 8.4 生辰八字取名
- 出生信息输入（公历/农历、日期、时辰、性别、姓氏）
- 本地排盘：四柱干支、五行统计、喜用提示（简化规则）
- 按五行补益推荐名字，标注每个字的五行属性
- 收藏、历史保存

### 8.5 诗词取名
- 内置诗词名句库（按出处分类）
- 诗句检索、按意境筛选
- 选句提炼关键字、组合候选名
- 名字带出处原文与释义
- 收藏、历史保存

### 8.6 宠物取名
- 宠物信息设置（类型、性别、毛色、性格、风格）
- 风格化词库（萌系叠词、食物系、拟声系、洋气音译等）
- 中英文名切换
- 按宠物存档、收藏

### 8.7 名字解析及评分
- 任意名字输入
- 分项解析：字义寓意、五行、音律、字形、笔画、谐音检查
- 分项评分 + 总分 + 评语
- 历史保存

---

## 9. 第一版暂时不做的功能

- 联网功能（在线查重、网络热名、大模型联网取名）—— 明确不联网
- 手机端、多设备同步、登录账号、云备份
- 高度复杂的传统命理排盘（真正的子平八字/紫微斗数级精度；V1 用简化五行喜忌规则）
- 文献上传暂只支持 `.txt` 纯文本（Word 文档可另存为 txt；PDF 解析后续再说）
- 生僻字大全、全部汉字的多音字/繁简完整处理
- 社交分享、社区功能
- 对话式 AI 深度聊天取名（如需要可后续加本地模型）

---

## 10. 可以实际检查的验收标准

以下每条都是可实际操作验证的验收项：

### 10.1 启动与框架
- [ ] 双击/本地打开入口文件后，应用能在浏览器中正常启动，无需联网、无需登录。
- [ ] 顶栏、左侧导航（6 个入口）、主内容区布局完整，点击导航可切换对应页面。

### 10.2 数据持久化
- [ ] 在任意模块产生一条记录（取名/收藏/解析）后，刷新浏览器，数据仍在。
- [ ] 关闭浏览器标签页后重新打开，数据仍在。
- [ ] 重启电脑后再打开应用，数据仍在。

### 10.3 备份与恢复
- [ ] 点击"导出"，能下载一个 JSON 备份文件。
- [ ] 点击"导入"，选择该 JSON 文件后，原记录、收藏、素材、偏好均能恢复，且与导出前一致。

### 10.4 首页
- [ ] 快速 AI 取名卡片：选姓氏 + 场景 → 一键生成，能在卡片内直接看到结果。
- [ ] 上传文献取名：选择本地 `.txt` 文件 → 能提取出好字好词并生成候选名。
- [ ] 5 个模块摘要卡片均显示各自摘要（最近记录/收藏数等），点击可进入对应模块。
- [ ] 最近记录列表显示各模块最近记录，点击可跳转。

### 10.5 新生儿取名
- [ ] 输入姓氏、选择性别与风格 → 生成名字列表。
- [ ] 名字卡片展示拼音、笔画、五行、寓意。
- [ ] 点"换一批"能重新生成；按风格筛选、只看收藏可用。
- [ ] 输入避讳字后，结果中不出现包含该字的姓名。
- [ ] 设置字辈后，名字中间字固定为字辈字。

### 10.6 生辰八字取名
- [ ] 输入出生日期与时辰 → 能显示四柱干支、五行统计、喜用/缺失提示。
- [ ] 推荐名字均按五行补益逻辑生成，且每个字标注五行属性。
- [ ] 不填时辰也能出结果（提示五行判断较粗略）。

### 10.7 诗词取名
- [ ] 诗词库按出处分类可浏览。
- [ ] 输入关键词能实时检索到相关诗句。
- [ ] 点选名句后能提炼关键字并组合出候选名。
- [ ] 每个名字显示出处原文与释义。

### 10.8 宠物取名
- [ ] 设置宠物类型/性别/毛色/性格/风格 → 生成宠物名字。
- [ ] 名字卡片展示含义与适合说明。
- [ ] 切换风格后词库来源随之变化。
- [ ] 中英文名切换可用。

### 10.9 名字解析及评分
- [ ] 输入任意名字 → 自动给出分项解析（字义/五行/音律/字形/笔画/谐音）。
- [ ] 显示分项评分 + 总分 + 一句评语。
- [ ] 对存在不良谐音的名字能给出提示。

### 10.10 收藏与历史
- [ ] 各模块星标收藏后，全局收藏夹中能统一看到。
- [ ] 各模块操作后，历史记录中能查到对应记录。
